# Copy this to .env and fill in. Do not commit the real .env.

PUID=1000
PGID=1000
TZ=America/New_York

# Host paths (Windows/Docker Desktop format shown — forward slashes work fine)
MOVIES_PATH=D:/movies
TV_PATH=D:/Tv Shows
DOWNLOADS_PATH=D:/downloads
CONFIG_ROOT=D:/appdata

# --- Gluetun / VPN ---
# See https://github.com/qdm12/gluetun-wiki/tree/main/setup for your provider's
# exact required vars. Below assumes WireGuard against a generic provider.
VPN_SERVICE_PROVIDER=protonvpn
VPN_TYPE=wireguard
WIREGUARD_PRIVATE_KEY=
WIREGUARD_ADDRESSES=
VPN_SERVER_COUNTRIES=

#QBT stuffs
QBITTORRENT_PASS=
QBITTORRENT_USER=

# --- Seedbox cross-seeding (ratio maintenance) ---
# Torrents in this qBittorrent category get auto-pushed to the seedbox on
# completion for independent seeding. Set this category on the download
# client entry tied to the private-tracker indexer(s) in Sonarr/Radarr.
RATIO_CATEGORY=ratio
# e.g. https://your-seedbox-host:port (seedbox's qBittorrent WebUI)
SEEDBOX_URL=
SEEDBOX_USER=
SEEDBOX_PASS=